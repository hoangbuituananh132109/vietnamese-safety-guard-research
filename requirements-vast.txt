# Vast.ai image should already provide a CUDA-compatible PyTorch build.
# Do not force-reinstall torch until nvidia-smi + torch.cuda are verified.
transformers==4.57.6
tokenizers==0.22.2
accelerate==1.14.0
peft==0.19.1
gliner2[local]==1.3.2
safetensors==0.8.0
numpy==2.4.6
scikit-learn==1.9.0
tqdm>=4.66
jupyterlab>=4.2
ipywidgets>=8.1
pytest>=9,<10
